Mark Task Master task as done.

Arguments: $ARGUMENTS

Execute the command:

```bash
npx task-master set-status --id=$ARGUMENTS --status=done
```

Usage: Provide task ID as argument (e.g., `/tm:set-status:to-done 9100`)
