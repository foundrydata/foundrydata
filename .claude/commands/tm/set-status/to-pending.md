Mark Task Master task as pending.

Arguments: $ARGUMENTS

Execute the command:

```bash
npx task-master set-status --id=$ARGUMENTS --status=pending
```

Usage: Provide task ID as argument (e.g., `/tm:set-status:to-pending 9100`)
