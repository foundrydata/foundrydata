Mark Task Master task as in-progress.

Arguments: $ARGUMENTS

Execute the command:

```bash
npx task-master set-status --id=$ARGUMENTS --status=in-progress
```

Usage: Provide task ID as argument (e.g., `/tm:set-status:to-in-progress 9100`)
