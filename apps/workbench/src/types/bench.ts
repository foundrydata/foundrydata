import type {
  BenchRunSummary,
  BenchSchemaSummary,
  BenchTotals,
  BenchLevel,
} from 'json-schema-reporter/bench/types';

export type { BenchRunSummary, BenchSchemaSummary, BenchTotals, BenchLevel };
