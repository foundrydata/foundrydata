{
  "name": "json-schema-reporter",
  "version": "0.1.0",
  "type": "module",
  "description": "JSON Schema reporting layer with CLI + Markdown/HTML renderers",
  "main": "dist/cli.js",
  "bin": {
    "json-schema-reporter": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc --build",
    "dev": "tsc --watch",
    "test": "vitest run"
  },
  "dependencies": {
    "@foundrydata/core": "^0.1.0",
    "commander": "^11.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vitest": "^3.2.4"
  },
  "license": "MIT",
  "author": "fstepho",
  "repository": {
    "type": "git",
    "url": "https://github.com/foundrydata/foundrydata",
    "directory": "packages/reporter"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
